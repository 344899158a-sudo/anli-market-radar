{
  "name": "anli-market-radar-pages",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh": "node scripts/generate-dashboard.mjs",
    "validate": "node scripts/validate-dashboard.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run refresh && npm run validate"
  }
}
